/**
 * UNA --> ? : <--Y dos puntos en medio de las opciones
 * EXPRESION ? 'SI ES TRUE' : 'SI ES FALSE';
 * 
 */
let edad = 16;
let acceso = edad >= 18 ? 'Permitir' : 'No Permitido';
console.log(acceso);
//es muy parecido
//como funciona a If
